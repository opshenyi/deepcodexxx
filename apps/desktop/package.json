{
  "name": "@deepcodex/desktop",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build && cross-env DEEPCODEX_WEB_URL=http://127.0.0.1:5173 electron .",
    "start": "electron ."
  },
  "dependencies": {
    "electron": "^39.2.7"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  }
}
